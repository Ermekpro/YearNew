<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Получаем выбор "келемін" или "келмеймін"
    $attendance = isset($_POST['attendance']) ? $_POST['attendance'] : '';
    
    // Имя участника
    $name = isset($_POST['name']) ? htmlspecialchars($_POST['name']) : '';

    // Проверка, если имя не введено, и выбран "Келемін" или "Келмеймін"
    if (empty($name)) {
        echo "Аты-жөніңізді енгізіңіз.";
        exit;  // Выход из скрипта, если имя не указано
    }

    // Формируем сообщение для почты
    $subject = "Ответ на опрос о мероприятии";
    if ($attendance == 'yes') {
        // Если выбрали "Келемін"
        $message = "Қатысу: Келемін\nАты-жөні: " . $name . "\n";
        // Ответ пользователю
        $response = "Сіздің қатысуыңыз тіркелді. Келгеніңіз үшін қуаныштымыз, күтеміз!";
    } else {
        // Если выбрали "Келмеймін"
        $message = "Қатысу: Келмеймін\nАты-жөні: " . $name . "\n";
        // Ответ пользователю
        $response = "Сіздің қатыспайтындығыңыз тіркелді. Атыңыз: " . $name . ". Келесі жолы күтеміз!";
    }

    $to = "mc.silentobserver@gmail.com";  // Ваш email
    $headers = "From: no-reply@yourwebsite.com";

    // Отправляем письмо
    if (mail($to, $subject, $message, $headers)) {
        // Отправляем ответ участнику о том, что данные приняты
        echo $response;
    } else {
        echo "Қате болды, жауапты жіберу мүмкін емес.";
    }
}
?>